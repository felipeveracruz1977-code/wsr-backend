/**
 * @wsr/contracts — Contrato Único de Datos (SSOT) de Woman Social Run.
 *
 * Tipos canónicos generados desde el esquema de producción de Supabase
 * (proyecto enlazado) mediante `supabase gen types typescript --linked`.
 *
 * NO editar database.types.ts a mano: regenerarlo desde el esquema.
 */

export type {
  Json,
  Database,
  Tables,
  TablesInsert,
  TablesUpdate,
  Enums,
  CompositeTypes,
} from "./database.types";

export { Constants } from "./database.types";
